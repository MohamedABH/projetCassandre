{% extends 'base.html.twig' %}

{% block title %}Observation{% endblock %}

{% block body %}
    <h1>Observation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ observation.id }}</td>
            </tr>
            <tr>
                <th>Description_observation</th>
                <td>{{ observation.descriptionObservation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_observation_index') }}">back to list</a>

    <a href="{{ path('app_observation_edit', {'id': observation.id}) }}">edit</a>

    {{ include('observation/_delete_form.html.twig') }}
{% endblock %}
