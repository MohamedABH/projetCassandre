<form method="post" action="{{ path('app_observation_delete', {'id': observation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ observation.id) }}">
    <button class="btn">Delete</button>
</form>
